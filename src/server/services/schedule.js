const { Schedule } = require("../models");

async function getSchedule(id) {
  // const { id, start, end, name } = await Schedule.findOne({
  //   where: {
  //     id
  //   }
  // });
  // return { id, start, end, name };
}

export { getSchedule };
